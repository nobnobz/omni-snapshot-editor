(globalThis.TURBOPACK || (globalThis.TURBOPACK = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/src/lib/catalog-fallbacks.ts [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CATALOG_FALLBACKS",
    ()=>CATALOG_FALLBACKS
]);
const CATALOG_FALLBACKS = {
    "mdblist.2236": "[Header] Movies for Header",
    "mdblist.11754": "[Top Row] 30 Movies of the Week",
    "mdblist.11753": "[Top Row] 30 Shows of the Week",
    "mdblist.88302": "[Discover] Trending Movies",
    "mdblist.88303": "[Discover] Trending Shows",
    "mdblist.86934": "[Discover] Latest Movies",
    "mdblist.86710": "[Discover] LatestShows",
    "mdblist.86752": "[Service] Netflix Movies",
    "mdblist.86751": "[Service] Netflix Shows",
    "mdblist.86759": "[Service] Disney+ Movies",
    "mdblist.86758": "[Service] Disney+ Shows",
    "mdblist.86757": "[Service] Apple TV Movies",
    "mdblist.86625": "[Service] Apple TV Shows",
    "mdblist.86755": "[Service] Amazon Prime Movies",
    "mdblist.86753": "[Service] Amazon Prime Shows",
    "mdblist.89647": "[Service] HBO Max Movies",
    "mdblist.89649": "[Service] HBO Max Shows",
    "mdblist.88330": "[Service] Paramount+ Movies",
    "mdblist.86761": "[Service]  Paramount+ Shows",
    "mdblist.127955": "[Service] Hulu Movies",
    "mdblist.127954": "[Service] Hulu Shows",
    "mdblist.83487": "[Service] Peacock Movies",
    "mdblist.83484": "[Service] Peacock Shows",
    "streaming.cru": "[Service] Crunchyroll Shows",
    "mdblist.2407": "[Genre] Action Movies",
    "mdblist.91213": "[Genre] Action Shows",
    "mdblist.116037": "[Genre] Animated Movies",
    "mdblist.116038": "[Genre] Animated Shows",
    "mdblist.88468": "[Genre] Anime Movies",
    "mdblist.88469": "[Genre] Anime Shows",
    "mdblist.5771": "[Genre] Bollywood Movies",
    "mdblist.8113": "[Genre] Christmas Movies",
    "mdblist.126338": "[Genre] Christmas Movies 2",
    "mdblist.2195": "[Genre] Comedy Movies",
    "mdblist.91224": "[Genre] Comedy Shows",
    "mdblist.2411": "[Genre] Crime Movies",
    "mdblist.3126": "[Genre] Crime Shows",
    "mdblist.84677": "[Genre] Documentary Movies",
    "mdblist.84403": "[Genre] Documentary Shows",
    "mdblist.2408": "[Genre] Drama Movies",
    "mdblist.91297": "[Genre] Drama Shows",
    "mdblist.2415": "[Genre] Family Movies",
    "mdblist.6605": "[Genre] Family Shows",
    "mdblist.2412": "[Genre] Fantasy/Sci-Fi Movies",
    "mdblist.91221": "[Genre] Fantasy/Sci-Fi Shows",
    "mdblist.2410": "[Genre] Horror Movies",
    "mdblist.91217": "[Genre] Horror Shows",
    "mdblist.130778": "[Genre] KDrama Movies",
    "mdblist.7577": "[Genre] KDrama Shows",
    "mdblist.3892": "[Genre] Mindfuck Movies",
    "mdblist.2416": "[Genre] Mystery Movies",
    "mdblist.6614": "[Genre] Mystery Shows",
    "mdblist.128261": "[Genre] Romance Movies",
    "mdblist.6617": "[Genre] Romance Shows",
    "mdblist.136620": "[Genre] Seasonal",
    "mdblist.91893": "[Genre] Thriller Movies",
    "mdblist.91894": "[Genre] Thriller Shows",
    "mdblist.84401": "[Genre] Top Reality TV",
    "mdblist.136229": "[Genre] Österreichischer Film",
    "mdblist.136107": "[Genre] Österreichische Serien",
    "mdblist.146022": "[Collection] Alien",
    "mdblist.143867": "[Collection] Avatar",
    "mdblist.98039": "[Collection] DC Movies",
    "mdblist.105130": "[Collection] DC Shows",
    "mdblist.143866": "[Collection] Dune",
    "mdblist.143885": "[Collection] Eberhofer Krimis",
    "mdblist.59631": "[Collection] Fast and Furious",
    "mdblist.105063": "[Collection] Harry Potter",
    "mdblist.143868": "[Collection] Hunger Games",
    "mdblist.101881": "[Collection] IMDb Top 250 Movies",
    "mdblist.101882": "[Collection] IMDb Top 250 Shows",
    "mdblist.125120": "[Collection] Indiana Jones",
    "mdblist.7947": "[Collection] James Bond",
    "mdblist.143882": "[Collection] John Wick Universe",
    "mdblist.125146": "[Collection] Jurassic Park",
    "mdblist.94304": "[Collection] Lord of the Rings and Hobbit",
    "mdblist.3022": "[Collection] Marvel Movies",
    "mdblist.128063": "[Collection] Marvel Shows",
    "mdblist.98353": "[Collection] Matrix",
    "mdblist.31309": "[Collection] Mission Impossible",
    "mdblist.125124": "[Collection] Pirates of the Caribbean",
    "mdblist.31324": "[Collection] Rambo",
    "mdblist.143870": "[Collection] Star Trek Universe",
    "mdblist.143863": "[Collection] Star Wars Universe",
    "mdblist.125458": "[Collection] Terminator Universe",
    "mdblist.143865": "[Collection] The Godfather",
    "mdblist.8682": "[Collection] Transformers",
    "mdblist.149616": "[Awards] Cannes Palme d'Or",
    "mdblist.149620": "[Awards] Emmy Awards 2000-2025",
    "mdblist.149615": "[Awards] Golden Globes Winners",
    "mdblist.100467": "[Awards] Oscar Winners",
    "mdblist.140566": "[Awards] Oscars 2026 Nominations",
    "mdblist.91304": "[Year] Popular 2020s Movies",
    "mdblist.91303": "[Year] Popular 2010s Movies",
    "mdblist.91302": "[Year] Popular 2000s Movies",
    "mdblist.91300": "[Year] Popular 1990s Movies",
    "mdblist.91301": "[Year] Popular 1980s Movies",
    "mdblist.2820": "[Year] Popular 1970s Movies",
    "mdblist.143650": "[Actor] Adam Sandler Movies",
    "mdblist.143643": "[Actor] Angelina Jolie Movies",
    "mdblist.143653": "[Actor] Anne Hathaway Movies",
    "mdblist.143690": "[Actor] Benedict Cumberbatch Movies",
    "mdblist.143718": "[Actor] Benedict Cumberbatch Shows",
    "mdblist.143659": "[Actor] Ben Stiller Movies",
    "mdblist.143658": "[Actor] Bill Murray Movies",
    "mdblist.143642": "[Actor] Brad Pitt Movies",
    "mdblist.143656": "[Actor] Channing Tatum Movies",
    "mdblist.136686": "[Actor] Channing Tatum Shows",
    "mdblist.143663": "[Actor] Daniel Craig Movies",
    "mdblist.145242": "[Actor] Denzel Washington Movies",
    "mdblist.143639": "[Actor] Dwayne Johnson Movies",
    "mdblist.142243": "[Actor] Dwayne Johnson Shows",
    "mdblist.143677": "[Actor] Emma Stone Movies",
    "mdblist.136709": "[Actor] Emma Stone Shows",
    "mdblist.143673": "[Actor] Florence Pugh Movies",
    "mdblist.143716": "[Actor] Florence Pugh Shows",
    "mdblist.143657": "[Actor] George Clooney Movies",
    "mdblist.143720": "[Actor] George Clooney Shows",
    "mdblist.143648": "[Actor] Harrison Ford Movies",
    "mdblist.143706": "[Actor] Harrison Ford Shows",
    "mdblist.143671": "[Actor] Jack Black Movies",
    "mdblist.136700": "[Actor] Jack Black Shows",
    "mdblist.143646": "[Actor] Jason Statham Movies",
    "mdblist.143688": "[Actor] Jenna Ortega Movies",
    "mdblist.143705": "[Actor] Jenna Ortega Shows",
    "mdblist.143664": "[Actor] Jennifer Aniston Movies",
    "mdblist.143708": "[Actor] Jennifer Aniston Shows",
    "mdblist.143641": "[Actor] Jennifer Lawrance Movies",
    "mdblist.143654": "[Actor] Jim Carrey Movies",
    "mdblist.143637": "[Actor] Johnny Depp Movies",
    "mdblist.143373": "[Actor] Johnny Depp Shows",
    "mdblist.143661": "[Actor] Jonah Hill Movies",
    "mdblist.143691": "[Actor] Julia Roberts Movies",
    "mdblist.143358": "[Actor] Julia Roberts Shows",
    "mdblist.143672": "[Actor] Katherine Heigl Movies",
    "mdblist.136706": "[Actor] Katherine Heigl Shows",
    "mdblist.143687": "[Actor] Leonardo DiCaprio Movies",
    "mdblist.143675": "[Actor] Lily Collins Movies",
    "mdblist.143714": "[Actor] Lily Collins Shows",
    "mdblist.143662": "[Actor] Margot Robbie Movies",
    "mdblist.143645": "[Actor] Matt Damon Movies",
    "mdblist.143668": "[Actor] Matthew McConaughey Movies",
    "mdblist.136684": "[Actor] Matthew McConaughey Shows",
    "mdblist.143670": "[Actor] Melissa McCarthy Movies",
    "mdblist.136692": "[Actor] Melissa McCarthy Shows",
    "mdblist.143694": "[Actor] Meryl Streep Movies",
    "mdblist.143680": "[Actor] Michael \"Bully\" Herbig Movies",
    "mdblist.136695": "[Actor] Michael „Bully“ Herbig Shows",
    "mdblist.143666": "[Actor] Mila Kunis Movies",
    "mdblist.136678": "[Actor] Mila Kunis Shows",
    "mdblist.143665": "[Actor] Millie Bobby Brown Movies",
    "mdblist.143710": "[Actor] Millie Bobby Brown Shows",
    "mdblist.143651": "[Actor] Morgan Freeman Movies",
    "mdblist.143652": "[Actor] Nicolas Cage Movies",
    "mdblist.143715": "[Actor] Nicolas Cage Shows",
    "mdblist.143681": "[Actor] Nicole Kidman Movies",
    "mdblist.143704": "[Actor] Nicole Kidman Shows",
    "mdblist.143660": "[Actor] Owen Wilson Movies",
    "mdblist.143667": "[Actor] Reese Witherspoon Movies",
    "mdblist.143709": "[Actor] Reese Witherspoon Shows",
    "mdblist.143696": "[Actor] Robert Downey Jr. Movies",
    "mdblist.143365": "[Actor] Robert Downey Jr. Shows",
    "mdblist.143682": "[Actor] Robin Williams Movies",
    "mdblist.142242": "[Actor] Robin Williams Shows",
    "mdblist.143689": "[Actor] Ryan Reynolds Movies",
    "mdblist.143644": "[Actor] Samuel L. Jackson Movies",
    "mdblist.143717": "[Actor] Samuel L. Jackson Shows",
    "mdblist.143640": "[Actor] Sandra Bullock Movies",
    "mdblist.143669": "[Actor] Steve Carell Movies",
    "mdblist.143712": "[Actor] Steve Carell Shows",
    "mdblist.143678": "[Actor] Sydney Sweeney Movies",
    "mdblist.143713": "[Actor] Sydney Sweeney Shows",
    "mdblist.147855": "[Actor] Timothée Chalamet Movies & Shows",
    "mdblist.143692": "[Actor] Tom Cruise Movies",
    "mdblist.143685": "[Actor] Tom Holland Movies",
    "mdblist.143374": "[Actor] Tom Holland Shows",
    "mdblist.143686": "[Actor] Zendaya Movies",
    "mdblist.142241": "[Actor] Zendaya Shows",
    "mdblist.53976": "[Director] Alfred Hitchcock",
    "mdblist.143632": "[Director] Christopher Nolan",
    "mdblist.91029": "[Director] David Fincher",
    "mdblist.150391": "[Director] David Lynch",
    "mdblist.143635": "[Director] Denis Villeneuve Movies",
    "mdblist.75977": "[Director] Paul Thomas Anderson",
    "mdblist.75976": "[Director] Francis Ford Coppola",
    "mdblist.75982": "[Director] Guillermo del Toro",
    "mdblist.143633": "[Director] James Cameron",
    "mdblist.143634": "[Director] John Carpenter",
    "mdblist.106355": "[Director] Martin Scorsese",
    "mdblist.85399": "[Director] Peter Jackson",
    "mdblist.143629": "[Director] Quentin Tarantino",
    "mdblist.143630": "[Director] Ridley Scott",
    "mdblist.149984": "[Director] Spike Lee",
    "mdblist.91028": "[Director] Stanley Kubrick",
    "mdblist.143631": "[Director] Steven Spielberg",
    "mdblist.119708": "[Director] Wes Anderson",
    "mdblist.87667": "[Top] Trakt's Trending Movies",
    "mdblist.88434": "[Top] Trakt's Trending Shows",
    "mdblist.88328": "[Top] Netflix Movies",
    "mdblist.88329": "[Top] Netflix Shows",
    "mdblist.3095": "[Top] Disney+ Movies",
    "mdblist.88323": "[Top] Disney+ Shows",
    "mdblist.88317": "[Top] Apple TV Movies",
    "mdblist.7444": "[Top] Apple TV Shows",
    "mdblist.88332": "[Top] Prime Movies",
    "mdblist.88333": "[Top] Prime Shows",
    "mdblist.88324": "[Top] HBO Max Movies",
    "mdblist.88325": "[Top] HBO Max Shows",
    "mdblist.89366": "[Top] Paramount+ Movies",
    "mdblist.88331": "[Top] Paramount+ Shows"
};
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/src/lib/utils.ts [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "cn",
    ()=>cn,
    "ensureCatalogPrefix",
    ()=>ensureCatalogPrefix,
    "formatDisplayName",
    ()=>formatDisplayName,
    "resolveCatalogName",
    ()=>resolveCatalogName
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$clsx$2f$dist$2f$clsx$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/clsx/dist/clsx.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tailwind$2d$merge$2f$dist$2f$bundle$2d$mjs$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tailwind-merge/dist/bundle-mjs.mjs [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$catalog$2d$fallbacks$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/catalog-fallbacks.ts [app-client] (ecmascript)");
;
;
;
function cn(...inputs) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tailwind$2d$merge$2f$dist$2f$bundle$2d$mjs$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["twMerge"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$clsx$2f$dist$2f$clsx$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["clsx"])(inputs));
}
function formatDisplayName(name) {
    if (!name) return name;
    try {
        // Fix for UTF-8 bytes misinterpreted as ISO-8859-1 (Mojibake)
        // Common cases: Michael „Bully“ Herbig, Timothée Chalamet
        return decodeURIComponent(escape(name));
    } catch (e) {
        // If not valid Mojibake or already correct, return as is
        return name;
    }
}
function resolveCatalogName(id, customNames = {}) {
    if (!id) return id;
    // 1. Exact Custom Name from JSON
    const customName = customNames[id];
    if (customName && customName.trim() !== "" && customName !== id) {
        return formatDisplayName(customName);
    }
    // Helper to strip leading prefixes like "movie:", "series:"
    let baseId = id;
    if (id.includes(":")) {
        const parts = id.split(":");
        if (parts.length === 2 && [
            "movie",
            "series",
            "anime"
        ].includes(parts[0])) {
            baseId = parts[1];
        } else if (!id.startsWith("movie:trakt-list") && !id.startsWith("series:trakt-list")) {
            baseId = parts.slice(1).join(":");
        }
    }
    // 1b. Fallback Custom Name (without prefix)
    const fallbackCustomName = customNames[baseId];
    if (fallbackCustomName && fallbackCustomName.trim() !== "" && fallbackCustomName !== baseId) {
        return formatDisplayName(fallbackCustomName);
    }
    // 2. Fallback from Metadata List (exact or base)
    const fallbackName = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$catalog$2d$fallbacks$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["CATALOG_FALLBACKS"][id] || __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$catalog$2d$fallbacks$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["CATALOG_FALLBACKS"][baseId];
    if (fallbackName) {
        return formatDisplayName(fallbackName);
    }
    // 3. Last resort: ID itself
    return id;
}
function ensureCatalogPrefix(id, name) {
    if (!id || id.includes(':')) return id;
    const lowerName = (name || "").toLowerCase();
    let typePrefix = "movie:"; // default
    if (lowerName.includes("show") || lowerName.includes("series") || lowerName.includes("tv")) {
        typePrefix = "series:";
    }
    return `${typePrefix}${id}`;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/src/lib/config-utils.ts [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Utilities for decoding and encoding the _data base64 strings in the Omni Config.
 */ // Helper to check if an object is a _data wrapper
__turbopack_context__.s([
    "decodeConfig",
    ()=>decodeConfig,
    "encodeConfig",
    ()=>encodeConfig,
    "isBase64DataNode",
    ()=>isBase64DataNode,
    "pruneDisabledCatalogs",
    ()=>pruneDisabledCatalogs,
    "pruneDisabledKeys",
    ()=>pruneDisabledKeys
]);
const isBase64DataNode = (node)=>{
    return node && typeof node === "object" && !Array.isArray(node) && Object.keys(node).length === 1 && typeof node._data === "string";
};
const decodeConfig = (obj)=>{
    if (obj === null || obj === undefined) return obj;
    if (Array.isArray(obj)) {
        return obj.map((item)=>decodeConfig(item));
    }
    if (typeof obj === "object") {
        if (isBase64DataNode(obj)) {
            try {
                const decodedStr = atob(obj._data);
                const parsed = JSON.parse(decodedStr);
                // We do not recursively decode here unless we expect nested base64 (usually not the case)
                return parsed;
            } catch (e) {
                console.error("Failed to decode or parse base64 data", obj._data, e);
                return obj; // Return original if it fails
            }
        }
        const result = {};
        for (const [key, value] of Object.entries(obj)){
            result[key] = decodeConfig(value);
        }
        return result;
    }
    return obj;
};
const encodeConfig = (currentParsedMap, originalValuesMap, disabledKeys)=>{
    const result = {};
    // List of keys that Omni EXPECTS to be base64 wrapped even if they weren't in original
    const ALWAYS_WRAPPED_KEYS = [
        "mdblist_enabled_ratings",
        "selected_catalogs",
        "pinned_catalogs",
        "small_catalogs",
        "top_row_catalogs",
        "starred_catalogs",
        "randomized_catalogs",
        "small_toprow_catalogs",
        "catalog_ordering",
        "custom_catalog_names",
        "regex_pattern_custom_names",
        "regex_pattern_image_urls",
        "pattern_tag_enabled_patterns",
        "pattern_default_filter_enabled_patterns",
        "pattern_image_color_indices",
        "pattern_border_radius_indices",
        "pattern_background_opacities",
        "pattern_border_thickness_indices",
        "pattern_color_indices",
        "pattern_color_hex_values",
        "auto_play_enabled_patterns",
        "auto_play_patterns",
        "landscape_catalogs",
        "disabled_shelves",
        "subtitle_color",
        "subtitle_background_color",
        "top_row_item_limits",
        "shelf_order",
        "subgroup_order",
        "main_catalog_groups",
        "catalog_groups",
        "catalog_group_image_urls"
    ];
    for (const [key, value] of Object.entries(currentParsedMap)){
        const originalValue = originalValuesMap[key];
        const shouldBeWrapped = isBase64DataNode(originalValue) || ALWAYS_WRAPPED_KEYS.includes(key);
        if (shouldBeWrapped) {
            try {
                const stringified = JSON.stringify(value);
                const encoded = btoa(stringified);
                result[key] = {
                    _data: encoded
                };
            } catch (e) {
                console.error("Failed to encode data for key", key, e);
                result[key] = value;
            }
        } else {
            // It wasn't base64 originally and isn't on our must-wrap list
            result[key] = value;
        }
    }
    return result;
};
const pruneDisabledCatalogs = (values, disabledCatalogs)=>{
    // Deep clone to avoid mutating state directly during export
    const cloned = JSON.parse(JSON.stringify(values));
    const pruneArray = (arr)=>arr.filter((item)=>{
            if (typeof item === 'string') {
                return !disabledCatalogs.has(item);
            }
            // If it's an object with an 'id' or something similar
            if (item && typeof item === 'object' && item.id) {
                return !disabledCatalogs.has(item.id);
            }
            return true;
        });
    // Recursive search and prune arrays
    const walkAndPrune = (obj)=>{
        if (Array.isArray(obj)) {
            return pruneArray(obj);
        }
        if (obj !== null && typeof obj === 'object') {
            for(const key in obj){
                // If the key itself is a disabled group/catalog name, delete it.
                // This covers `catalog_groups[disabled]` and `catalog_group_image_urls[disabled]`
                if (disabledCatalogs.has(key)) {
                    delete obj[key];
                    continue;
                }
                if (Array.isArray(obj[key])) {
                    obj[key] = pruneArray(obj[key]);
                } else if (typeof obj[key] === 'object') {
                    obj[key] = walkAndPrune(obj[key]);
                }
            }
        }
        return obj;
    };
    return walkAndPrune(cloned);
};
const pruneDisabledKeys = (values, disabledKeys)=>{
    const cloned = JSON.parse(JSON.stringify(values));
    const walkAndPrune = (obj, currentPath)=>{
        if (obj !== null && typeof obj === 'object' && !Array.isArray(obj)) {
            for(const key in obj){
                const pathStr = [
                    ...currentPath,
                    key
                ].join('.');
                if (disabledKeys.has(pathStr)) {
                    delete obj[key];
                    continue;
                }
                obj[key] = walkAndPrune(obj[key], [
                    ...currentPath,
                    key
                ]);
            }
        }
        return obj;
    };
    return walkAndPrune(cloned, []);
};
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/src/lib/mutations.ts [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "assignSubgroup",
    ()=>assignSubgroup,
    "countGroupReferences",
    ()=>countGroupReferences,
    "countMainGroupReferences",
    ()=>countMainGroupReferences,
    "createMainGroup",
    ()=>createMainGroup,
    "createSubgroup",
    ()=>createSubgroup,
    "disableCatalog",
    ()=>disableCatalog,
    "disableGroup",
    ()=>disableGroup,
    "disableMainGroup",
    ()=>disableMainGroup,
    "getAllCatalogIds",
    ()=>getAllCatalogIds,
    "importGroups",
    ()=>importGroups,
    "renameGroup",
    ()=>renameGroup,
    "renameMainGroup",
    ()=>renameMainGroup,
    "unassignSubgroup",
    ()=>unassignSubgroup,
    "validateAndFix",
    ()=>validateAndFix
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/utils.ts [app-client] (ecmascript)");
;
function countGroupReferences(name, state) {
    let count = 0;
    if (state.catalog_groups && state.catalog_groups[name]) count++;
    if (state.catalog_group_order && state.catalog_group_order.includes(name)) count++;
    if (state.catalog_group_image_urls && state.catalog_group_image_urls[name] !== undefined) count++;
    if (state.subgroup_order) {
        Object.values(state.subgroup_order).forEach((arr)=>{
            if (Array.isArray(arr) && arr.includes(name)) count++;
        });
    }
    if (state.main_catalog_groups) {
        Object.values(state.main_catalog_groups).forEach((group)=>{
            if (group && Array.isArray(group.subgroupNames) && group.subgroupNames.includes(name)) count++;
        });
    }
    return count;
}
function countMainGroupReferences(uuid, state) {
    let count = 0;
    if (state.main_catalog_groups && state.main_catalog_groups[uuid]) count++;
    if (state.main_group_order && state.main_group_order.includes(uuid)) count++;
    if (state.subgroup_order && state.subgroup_order[uuid]) count++;
    return count;
}
function renameGroup(oldName, newName, state) {
    if (oldName === newName) return state;
    // We do NOT mutate the original state, make a deep copy or do immutable updates
    const draft = JSON.parse(JSON.stringify(state));
    const mergeArrays = (arr1 = [], arr2 = [])=>{
        return Array.from(new Set([
            ...arr1,
            ...arr2
        ])); // Quick unique merge
    };
    // 1. Rename the key in catalog_groups
    if (draft.catalog_groups) {
        if (draft.catalog_groups[oldName]) {
            const oldItems = draft.catalog_groups[oldName] || [];
            if (draft.catalog_groups[newName]) {
                // Merge logic if newName exists
                draft.catalog_groups[newName] = mergeArrays(draft.catalog_groups[newName], oldItems);
            } else {
                draft.catalog_groups[newName] = oldItems;
            }
            delete draft.catalog_groups[oldName];
        }
    }
    // 2. Replace the old name in catalog_group_order
    if (Array.isArray(draft.catalog_group_order)) {
        const index = draft.catalog_group_order.indexOf(oldName);
        if (index !== -1) {
            // If the target name already exists, we just filter out the oldName to prevent dupes
            if (draft.catalog_group_order.includes(newName)) {
                draft.catalog_group_order = draft.catalog_group_order.filter((g)=>g !== oldName);
            } else {
                draft.catalog_group_order[index] = newName;
            }
        }
    }
    // 3. Replace the old name in subgroup_order arrays
    if (draft.subgroup_order) {
        Object.keys(draft.subgroup_order).forEach((mainGroupUUID)=>{
            const arr = draft.subgroup_order[mainGroupUUID];
            if (Array.isArray(arr)) {
                const index = arr.indexOf(oldName);
                if (index !== -1) {
                    if (arr.includes(newName)) {
                        draft.subgroup_order[mainGroupUUID] = arr.filter((s)=>s !== oldName);
                    } else {
                        arr[index] = newName;
                    }
                }
            }
        });
    }
    // 4. Replace within main_catalog_groups[*].subgroupNames
    if (draft.main_catalog_groups) {
        Object.keys(draft.main_catalog_groups).forEach((uuid)=>{
            const group = draft.main_catalog_groups[uuid];
            if (group && Array.isArray(group.subgroupNames)) {
                const index = group.subgroupNames.indexOf(oldName);
                if (index !== -1) {
                    if (group.subgroupNames.includes(newName)) {
                        group.subgroupNames = group.subgroupNames.filter((s)=>s !== oldName);
                    } else {
                        group.subgroupNames[index] = newName;
                    }
                }
            }
        });
    }
    // 5. Rename the key in catalog_group_image_urls
    if (draft.catalog_group_image_urls) {
        if (draft.catalog_group_image_urls[oldName] !== undefined) {
            // Always overwrite target image url, or keep existing target if preferable.
            // Let's keep existing target if it exists, otherwise move old
            if (draft.catalog_group_image_urls[newName] === undefined) {
                draft.catalog_group_image_urls[newName] = draft.catalog_group_image_urls[oldName];
            }
            delete draft.catalog_group_image_urls[oldName];
        }
    }
    return draft;
}
function renameMainGroup(uuid, newName, state) {
    const draft = JSON.parse(JSON.stringify(state));
    if (draft.main_catalog_groups && draft.main_catalog_groups[uuid]) {
        draft.main_catalog_groups[uuid].name = newName;
    }
    return draft;
}
function unassignSubgroup(name, state) {
    const draft = JSON.parse(JSON.stringify(state));
    if (draft.subgroup_order) {
        Object.keys(draft.subgroup_order).forEach((mainGroupUUID)=>{
            const arr = draft.subgroup_order[mainGroupUUID];
            if (Array.isArray(arr)) {
                draft.subgroup_order[mainGroupUUID] = arr.filter((s)=>s !== name);
            }
        });
    }
    if (draft.main_catalog_groups) {
        Object.keys(draft.main_catalog_groups).forEach((uuid)=>{
            const group = draft.main_catalog_groups[uuid];
            if (group && Array.isArray(group.subgroupNames)) {
                group.subgroupNames = group.subgroupNames.filter((s)=>s !== name);
            }
        });
    }
    return draft;
}
function assignSubgroup(name, targetMainGroupUuid, state) {
    const draft = JSON.parse(JSON.stringify(state));
    // First unassign it from any existing main groups to ensure it only lives in one place
    if (draft.subgroup_order) {
        Object.keys(draft.subgroup_order).forEach((mainGroupUUID)=>{
            const arr = draft.subgroup_order[mainGroupUUID];
            if (Array.isArray(arr)) {
                draft.subgroup_order[mainGroupUUID] = arr.filter((s)=>s !== name);
            }
        });
    }
    if (draft.main_catalog_groups) {
        Object.keys(draft.main_catalog_groups).forEach((uuid)=>{
            const group = draft.main_catalog_groups[uuid];
            if (group && Array.isArray(group.subgroupNames)) {
                group.subgroupNames = group.subgroupNames.filter((s)=>s !== name);
            }
        });
    }
    // Now assign to the new parent
    if (!draft.main_catalog_groups[targetMainGroupUuid]) {
        draft.main_catalog_groups[targetMainGroupUuid] = {
            name: "Unknown Group",
            subgroupNames: []
        };
    }
    if (!Array.isArray(draft.main_catalog_groups[targetMainGroupUuid].subgroupNames)) {
        draft.main_catalog_groups[targetMainGroupUuid].subgroupNames = [];
    }
    // Add to the end of the new parent's list
    draft.main_catalog_groups[targetMainGroupUuid].subgroupNames.push(name);
    // ALSO keep subgroup_order in sync (this is what the editor reads from to render subgroups)
    if (!draft.subgroup_order) draft.subgroup_order = {};
    if (!Array.isArray(draft.subgroup_order[targetMainGroupUuid])) {
        draft.subgroup_order[targetMainGroupUuid] = [];
    }
    if (!draft.subgroup_order[targetMainGroupUuid].includes(name)) {
        draft.subgroup_order[targetMainGroupUuid].push(name);
    }
    return draft;
}
function createMainGroup(name, assignedSubgroups, state) {
    const draft = JSON.parse(JSON.stringify(state));
    const newUuid = crypto.randomUUID();
    if (!draft.main_catalog_groups) draft.main_catalog_groups = {};
    if (!draft.main_group_order) draft.main_group_order = [];
    if (!draft.subgroup_order) draft.subgroup_order = {};
    draft.main_catalog_groups[newUuid] = {
        name,
        subgroupNames: [],
        posterSize: "Default",
        posterType: "Square"
    };
    draft.main_group_order.push(newUuid);
    // Now securely assign the selected subgroups
    let tempDraft = draft;
    for (const sg of assignedSubgroups){
        tempDraft = assignSubgroup(sg, newUuid, tempDraft);
    }
    return tempDraft;
}
function createSubgroup(name, targetMainGroupUuid, imageUrl, initialCatalogs = [], state) {
    const draft = JSON.parse(JSON.stringify(state));
    if (!draft.catalog_groups) draft.catalog_groups = {};
    if (!draft.catalog_group_order) draft.catalog_group_order = [];
    if (!draft.catalog_group_image_urls) draft.catalog_group_image_urls = {};
    // Create the subgroup with the provided catalog list
    const normalizedCatalogs = initialCatalogs.map((id)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ensureCatalogPrefix"])(id, (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["resolveCatalogName"])(id, draft.custom_catalog_names || {})));
    draft.catalog_groups[name] = normalizedCatalogs;
    draft.catalog_group_order.push(name);
    if (imageUrl) {
        draft.catalog_group_image_urls[name] = imageUrl;
    }
    // Now assign it to the selected main group
    if (targetMainGroupUuid) {
        return assignSubgroup(name, targetMainGroupUuid, draft);
    }
    return draft;
}
function disableGroup(name, state) {
    const draft = JSON.parse(JSON.stringify(state));
    if (draft.catalog_groups) {
        delete draft.catalog_groups[name];
    }
    if (Array.isArray(draft.catalog_group_order)) {
        draft.catalog_group_order = draft.catalog_group_order.filter((g)=>g !== name);
    }
    if (draft.subgroup_order) {
        Object.keys(draft.subgroup_order).forEach((mainGroupUUID)=>{
            const arr = draft.subgroup_order[mainGroupUUID];
            if (Array.isArray(arr)) {
                draft.subgroup_order[mainGroupUUID] = arr.filter((s)=>s !== name);
            }
            // If empty, we can optionally delete the uuid key from subgroup_order
            if (draft.subgroup_order[mainGroupUUID].length === 0) {
                delete draft.subgroup_order[mainGroupUUID];
            }
        });
    }
    if (draft.main_catalog_groups) {
        Object.keys(draft.main_catalog_groups).forEach((uuid)=>{
            const group = draft.main_catalog_groups[uuid];
            if (group && Array.isArray(group.subgroupNames)) {
                group.subgroupNames = group.subgroupNames.filter((s)=>s !== name);
            }
        });
    }
    if (draft.catalog_group_image_urls) {
        delete draft.catalog_group_image_urls[name];
    }
    return draft;
}
function disableMainGroup(uuid, state) {
    const draft = JSON.parse(JSON.stringify(state));
    // 1. Remove from main_group_order
    if (Array.isArray(draft.main_group_order)) {
        draft.main_group_order = draft.main_group_order.filter((id)=>id !== uuid);
    }
    // 2. Cascade delete subgroups
    const subgroupNames = draft.main_catalog_groups?.[uuid]?.subgroupNames || draft.subgroup_order?.[uuid] || [];
    if (Array.isArray(subgroupNames)) {
        subgroupNames.forEach((name)=>{
            if (draft.catalog_groups) delete draft.catalog_groups[name];
            if (Array.isArray(draft.catalog_group_order)) {
                draft.catalog_group_order = draft.catalog_group_order.filter((g)=>g !== name);
            }
            if (draft.catalog_group_image_urls) delete draft.catalog_group_image_urls[name];
        });
    }
    // 3. Remove main group entries
    if (draft.main_catalog_groups) {
        delete draft.main_catalog_groups[uuid];
    }
    if (draft.subgroup_order) {
        delete draft.subgroup_order[uuid];
    }
    return draft;
}
function disableCatalog(catalogId, state) {
    const draft = JSON.parse(JSON.stringify(state));
    // Remove from catalog_groups lists
    if (draft.catalog_groups) {
        Object.keys(draft.catalog_groups).forEach((groupName)=>{
            const arr = draft.catalog_groups[groupName];
            if (Array.isArray(arr)) {
                draft.catalog_groups[groupName] = arr.filter((c)=>c !== catalogId);
            }
        });
    }
    // Remove from common flat catalog arrays
    const commonLists = [
        "selected_catalogs",
        "pinned_catalogs",
        "small_catalogs",
        "top_row_catalogs",
        "starred_catalogs",
        "randomized_catalogs",
        "small_toprow_catalogs",
        "catalog_ordering"
    ];
    commonLists.forEach((listName)=>{
        if (Array.isArray(draft[listName])) {
            draft[listName] = draft[listName].filter((c)=>c !== catalogId);
        }
    });
    return draft;
}
function validateAndFix(state) {
    const draft = JSON.parse(JSON.stringify(state));
    const validGroupNames = new Set(Object.keys(draft.catalog_groups || {}));
    // Ensure catalog_group_order exists and is cleaned
    if (!Array.isArray(draft.catalog_group_order)) {
        draft.catalog_group_order = [];
    }
    // Fix corruption where order might be ["_data"]
    if (draft.catalog_group_order.length === 1 && draft.catalog_group_order[0] === "_data") {
        draft.catalog_group_order = [];
    }
    // Dedupe & Clean
    draft.catalog_group_order = Array.from(new Set(draft.catalog_group_order)).filter((g)=>typeof g === 'string' && validGroupNames.has(g));
    // Ensure every existing group appears
    validGroupNames.forEach((name)=>{
        if (!draft.catalog_group_order.includes(name)) {
            draft.catalog_group_order.push(name);
        }
    });
    // CRITICAL: Reorder the keys in catalog_groups and related objects to match catalog_group_order
    // This ensures that modern JSON stringifiers preserve the order requested by the user.
    if (draft.catalog_groups) {
        const orderedGroups = {};
        draft.catalog_group_order.forEach((name)=>{
            if (draft.catalog_groups[name]) {
                orderedGroups[name] = draft.catalog_groups[name];
            }
        });
        draft.catalog_groups = orderedGroups;
    }
    if (draft.catalog_group_image_urls) {
        const orderedUrls = {};
        draft.catalog_group_order.forEach((name)=>{
            if (draft.catalog_group_image_urls[name] !== undefined) {
                orderedUrls[name] = draft.catalog_group_image_urls[name];
            }
        });
        draft.catalog_group_image_urls = orderedUrls;
    }
    // Dedupe & Clean subgroup_order
    if (draft.subgroup_order) {
        Object.keys(draft.subgroup_order).forEach((uuid)=>{
            let arr = draft.subgroup_order[uuid];
            if (Array.isArray(arr)) {
                arr = Array.from(new Set(arr)).filter((g)=>typeof g === 'string' && validGroupNames.has(g));
                draft.subgroup_order[uuid] = arr;
                if (arr.length === 0) {
                    delete draft.subgroup_order[uuid];
                }
            }
        });
    }
    // Clean main_catalog_groups references
    if (draft.main_catalog_groups) {
        Object.keys(draft.main_catalog_groups).forEach((uuid)=>{
            const group = draft.main_catalog_groups[uuid];
            if (group && Array.isArray(group.subgroupNames)) {
                group.subgroupNames = Array.from(new Set(group.subgroupNames)).filter((g)=>typeof g === 'string' && validGroupNames.has(g));
            }
        });
    }
    // Dedupe catalog lists
    const commonLists = [
        "selected_catalogs",
        "pinned_catalogs",
        "small_catalogs",
        "top_row_catalogs",
        "starred_catalogs",
        "randomized_catalogs",
        "small_toprow_catalogs",
        "catalog_ordering"
    ];
    commonLists.forEach((listName)=>{
        if (Array.isArray(draft[listName])) {
            draft[listName] = Array.from(new Set(draft[listName]));
        }
    });
    return draft;
}
function importGroups(payload, state) {
    const draft = JSON.parse(JSON.stringify(state));
    if (!draft.main_catalog_groups) draft.main_catalog_groups = {};
    if (!draft.main_group_order) draft.main_group_order = [];
    if (!draft.catalog_groups) draft.catalog_groups = {};
    if (!draft.catalog_group_order) draft.catalog_group_order = [];
    if (!draft.catalog_group_image_urls) draft.catalog_group_image_urls = {};
    if (!draft.subgroup_order || Array.isArray(draft.subgroup_order)) {
        draft.subgroup_order = {};
    }
    // Build a reverse lookup: main group name -> existing UUID in current config
    const existingMgByName = {};
    Object.entries(draft.main_catalog_groups).forEach(([uid, mg])=>{
        if (mg?.name) existingMgByName[mg.name] = uid;
    });
    // 1. Import Main Groups
    Object.keys(payload.mainGroups).forEach((uuid)=>{
        const mg = payload.mainGroups[uuid];
        const incomingSubgroups = [
            ...mg.subgroupNames || []
        ];
        // If a group with the same NAME already exists, update its subgroup links
        // instead of creating a brand-new duplicate entry
        const existingUuid = existingMgByName[mg.name];
        if (existingUuid) {
            // Merge subgroup names into the existing group
            if (!draft.main_catalog_groups[existingUuid].subgroupNames) {
                draft.main_catalog_groups[existingUuid].subgroupNames = [];
            }
            incomingSubgroups.forEach((sgName)=>{
                if (!draft.main_catalog_groups[existingUuid].subgroupNames.includes(sgName)) {
                    draft.main_catalog_groups[existingUuid].subgroupNames.push(sgName);
                }
            });
            // Rebuild subgroup_order for the existing group (replace + merge)
            if (!draft.subgroup_order[existingUuid]) draft.subgroup_order[existingUuid] = [];
            incomingSubgroups.forEach((sgName)=>{
                if (!draft.subgroup_order[existingUuid].includes(sgName)) {
                    draft.subgroup_order[existingUuid].push(sgName);
                }
            });
        } else {
            // Brand new main group
            let targetUuid = uuid;
            if (draft.main_catalog_groups[targetUuid]) {
                targetUuid = crypto.randomUUID();
            }
            draft.main_catalog_groups[targetUuid] = {
                name: mg.name,
                subgroupNames: incomingSubgroups,
                posterType: mg.posterType,
                posterSize: mg.posterSize
            };
            draft.main_group_order.push(targetUuid);
            draft.subgroup_order[targetUuid] = incomingSubgroups;
        }
    });
    // 2. Import / Ensure Subgroups exist in catalog_groups
    Object.keys(payload.subgroups).forEach((name)=>{
        const sg = payload.subgroups[name];
        // Always ensure the catalog entry exists (create or update)
        const normalized = (sg.catalogs || []).map((id)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ensureCatalogPrefix"])(id, (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["resolveCatalogName"])(id, draft.custom_catalog_names || {})));
        draft.catalog_groups[name] = normalized;
        if (!draft.catalog_group_order.includes(name)) {
            draft.catalog_group_order.push(name);
        }
        if (sg.imageUrl) {
            draft.catalog_group_image_urls[name] = sg.imageUrl;
        }
        // Handle standalone assignment (subgroups not coming from a main group)
        const targetMainUuid = payload.standaloneAssignments[name];
        if (targetMainUuid && draft.main_catalog_groups[targetMainUuid]) {
            if (!draft.main_catalog_groups[targetMainUuid].subgroupNames) {
                draft.main_catalog_groups[targetMainUuid].subgroupNames = [];
            }
            if (!draft.main_catalog_groups[targetMainUuid].subgroupNames.includes(name)) {
                draft.main_catalog_groups[targetMainUuid].subgroupNames.push(name);
            }
            if (!draft.subgroup_order[targetMainUuid]) {
                draft.subgroup_order[targetMainUuid] = [];
            }
            if (!draft.subgroup_order[targetMainUuid].includes(name)) {
                draft.subgroup_order[targetMainUuid].push(name);
            }
        }
    });
    // 3. Import Metadata (Names, Images, Patterns)
    if (payload.metadata) {
        const meta = payload.metadata;
        if (meta.custom_catalog_names) {
            if (!draft.custom_catalog_names) draft.custom_catalog_names = {};
            Object.assign(draft.custom_catalog_names, meta.custom_catalog_names);
        }
        if (meta.regex_pattern_image_urls) {
            if (!draft.regex_pattern_image_urls) draft.regex_pattern_image_urls = {};
            Object.assign(draft.regex_pattern_image_urls, meta.regex_pattern_image_urls);
        }
        if (Array.isArray(meta.enabled_patterns)) {
            // Merge into both potential enable lists for coverage
            const lists = [
                "auto_play_enabled_patterns",
                "pattern_tag_enabled_patterns"
            ];
            lists.forEach((list)=>{
                if (!Array.isArray(draft[list])) draft[list] = [];
                meta.enabled_patterns.forEach((p)=>{
                    if (!draft[list].includes(p)) draft[list].push(p);
                });
            });
        }
    }
    // 4. Import Global Settings
    if (payload.globalSettings) {
        Object.assign(draft, payload.globalSettings);
    }
    return validateAndFix(draft);
}
function getAllCatalogIds(state) {
    const ids = new Set();
    const addIds = (source)=>{
        if (!source) return;
        if (Array.isArray(source)) {
            source.forEach((id)=>{
                if (typeof id === 'string') ids.add(id);
            });
        } else if (typeof source === 'object') {
            Object.values(source).forEach((val)=>{
                if (typeof val === 'string') ids.add(val);
                else if (Array.isArray(val)) val.forEach((id)=>{
                    if (typeof id === 'string') ids.add(id);
                });
            });
        }
    };
    // 1. Standard flat arrays
    const commonLists = [
        "selected_catalogs",
        "pinned_catalogs",
        "small_catalogs",
        "top_row_catalogs",
        "starred_catalogs",
        "randomized_catalogs",
        "small_toprow_catalogs"
    ];
    commonLists.forEach((list)=>addIds(state[list]));
    // 2. Complex structures
    addIds(state.catalog_ordering);
    if (state.catalog_groups) {
        Object.values(state.catalog_groups).forEach((arr)=>addIds(arr));
    }
    // 3. Known names (even if not in any list)
    if (state.custom_catalog_names) {
        Object.keys(state.custom_catalog_names).forEach((id)=>{
            if (!id.startsWith("_")) ids.add(id);
        });
    }
    return ids;
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/src/context/ConfigContext.tsx [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ConfigProvider",
    ()=>ConfigProvider,
    "useConfig",
    ()=>useConfig
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/compiled/react/jsx-dev-runtime.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/compiled/react/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/utils.ts [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/config-utils.ts [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/mutations.ts [app-client] (ecmascript)");
;
var _s = __turbopack_context__.k.signature(), _s1 = __turbopack_context__.k.signature();
"use client";
;
;
;
;
const ConfigContext = /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createContext"])(undefined);
const ConfigProvider = ({ children })=>{
    _s();
    const [originalConfig, setOriginalConfig] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])(null);
    const [initialValues, setInitialValues] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])({});
    const [currentValues, setCurrentValues] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])({});
    const [disabledKeys, setDisabledKeys] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])(new Set());
    const [disabledCatalogs, setDisabledCatalogs] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])(new Set());
    const [deletedSubgroups, setDeletedSubgroups] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])([]);
    const [deletedMainGroups, setDeletedMainGroups] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])([]);
    const [catalogs, setCatalogs] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])([]);
    const [fileName, setFileName] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])("omni-config.json");
    const [isSyntheticSession, setIsSyntheticSession] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])(false);
    // Custom fallbacks from localStorage
    const [customFallbacks, setCustomFallbacks] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useState"])({});
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].useEffect({
        "ConfigProvider.useEffect": ()=>{
            if ("TURBOPACK compile-time truthy", 1) {
                try {
                    const stored = localStorage.getItem("omni_custom_fallbacks");
                    if (stored) setCustomFallbacks(JSON.parse(stored));
                } catch (e) {
                    console.error("Failed to load custom fallbacks", e);
                }
            }
        }
    }["ConfigProvider.useEffect"], []);
    const loadConfig = (config, fn = "omni-config.json")=>{
        setOriginalConfig(config);
        setFileName(fn);
        // Map config.values OR config.config to internal values state
        const rawValues = config.values || config.config || {};
        const decodedValues = {};
        // Decode fields if they use the base64 wrapper format (_data)
        for (const [key, val] of Object.entries(rawValues)){
            // Remove mdblist setting for imported setups (keep only for clean template)
            if (fn !== "clear-config.json" && key === "mdblist_enabled_ratings") {
                continue;
            }
            decodedValues[key] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["decodeConfig"])(val);
        }
        // Force-inject new settings that might be missing from older configs
        if (decodedValues.hide_addon_info_in_catalog_names === undefined) {
            decodedValues.hide_addon_info_in_catalog_names = true;
        }
        // Extract catalogs if it's a manifest format (config.catalogs[])
        let extractedCatalogs = [];
        if (Array.isArray(config.config?.catalogs)) {
            extractedCatalogs = config.config.catalogs;
        } else if (Array.isArray(config.catalogs)) {
            extractedCatalogs = config.catalogs;
        }
        // FALLBACK: If no manifest catalogs found, synthesize minimal objects from state-format selected_catalogs
        if (extractedCatalogs.length === 0) {
            const decodedCatalogOrdering = decodedValues.catalog_ordering || decodedValues.selected_catalogs || [];
            const topRowList = decodedValues.top_row_catalogs || [];
            const customNames = decodedValues.custom_catalog_names || {};
            const topRowLimits = decodedValues.top_row_item_limits || {};
            // All IDs in scope
            const allIds = new Set([
                ...decodedCatalogOrdering,
                ...topRowList
            ]);
            extractedCatalogs = Array.from(allIds).map((id)=>{
                const name = customNames[id] || id;
                const finalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ensureCatalogPrefix"])(id, name);
                return {
                    id: finalId,
                    name: name,
                    enabled: decodedCatalogOrdering.length > 0 ? decodedCatalogOrdering.includes(id) : true,
                    showInHome: topRowList.includes(id),
                    metadata: topRowLimits[id] ? {
                        itemCount: topRowLimits[id]
                    } : undefined,
                    _synthetic: true
                };
            });
            setIsSyntheticSession(true);
        } else {
            setIsSyntheticSession(false);
        }
        setCatalogs(extractedCatalogs);
        setCurrentValues(decodedValues);
        setInitialValues(JSON.parse(JSON.stringify(decodedValues))); // Deep clone for safety
        // Populate disabledKeys from includedKeys if present
        const newDisabledKeys = new Set();
        const includedKeys = config.includedKeys || [];
        if (includedKeys.length > 0) {
            // Check top-level keys in values
            Object.keys(rawValues).forEach((k)=>{
                // Never disable certain new keys if they are just missing from an old config's includedKeys
                if (!includedKeys.includes(k) && k !== "hide_addon_info_in_catalog_names") {
                    newDisabledKeys.add(k);
                }
            });
        }
        setDisabledKeys(newDisabledKeys);
        setDisabledCatalogs(new Set());
        setDeletedSubgroups([]);
        setDeletedMainGroups([]);
    };
    const updateValuePath = (obj, path, value)=>{
        if (path.length === 0) return value;
        if (path.length === 1) {
            if (value === undefined) {
                const newObj = {
                    ...obj
                };
                delete newObj[path[0]];
                return newObj;
            }
            return {
                ...obj,
                [path[0]]: value
            };
        }
        const [head, ...rest] = path;
        const innerObj = obj ? obj[head] : {};
        return {
            ...obj,
            [head]: updateValuePath(innerObj, rest, value)
        };
    };
    const getValuePath = (obj, path)=>{
        return path.reduce((acc, part)=>acc && acc[part] !== undefined ? acc[part] : undefined, obj);
    };
    const updateValue = (keyPath, value)=>{
        setCurrentValues((prev)=>updateValuePath(prev, keyPath, value));
    };
    const toggleKey = (keyPath, isEnabled)=>{
        const keyString = keyPath.join(".");
        setDisabledKeys((prev)=>{
            const next = new Set(prev);
            if (isEnabled) {
                next.delete(keyString);
            } else {
                next.add(keyString);
            }
            return next;
        });
        if (isEnabled && originalConfig?.values) {
            // Only restore if it is NOT in currentValues (preserving session-level deletions/changes)
            const currentVal = getValuePath(currentValues, keyPath);
            if (currentVal === undefined) {
                const origVal = getValuePath(originalConfig.values, keyPath);
                if (origVal !== undefined) {
                    // Need to decode it just in case it was a base64 originally
                    updateValue(keyPath, (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["decodeConfig"])(origVal));
                }
            }
        }
    };
    const toggleCatalog = (catalogId, isEnabled)=>{
        setDisabledCatalogs((prev)=>{
            const next = new Set(prev);
            if (isEnabled) next.delete(catalogId);
            else next.add(catalogId);
            return next;
        });
    };
    const updateCatalogsOrder = (newOrder)=>{
    // Specifically for catalogs
    };
    const renameCatalogGroup = (oldName, newName)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["renameGroup"])(oldName, newName, prev));
    };
    const renameMainCatalogGroup = (uuid, newName)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["renameMainGroup"])(uuid, newName, prev));
    };
    const removeMainCatalogGroup = (uuid)=>{
        // Archive for Recycle Bin
        const group = currentValues.main_catalog_groups?.[uuid];
        const subgroupNames = currentValues.subgroup_order?.[uuid] || group?.subgroupNames || [];
        setDeletedMainGroups((prev)=>[
                {
                    uuid,
                    name: group?.name || "Group",
                    subgroupNames,
                    deletedAt: new Date().toISOString()
                },
                ...prev
            ]);
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["disableMainGroup"])(uuid, prev));
    };
    const restoreMainGroup = (item)=>{
        setCurrentValues((prev)=>{
            const draft = JSON.parse(JSON.stringify(prev));
            // 1. Restore the main group entry
            if (!draft.main_catalog_groups) draft.main_catalog_groups = {};
            draft.main_catalog_groups[item.uuid] = {
                name: item.name,
                subgroupNames: item.subgroupNames
            };
            // 2. Restore the subgroup order array
            if (!draft.subgroup_order) draft.subgroup_order = {};
            draft.subgroup_order[item.uuid] = item.subgroupNames;
            // 3. Add back to main_group_order if missing
            if (!draft.main_group_order) draft.main_group_order = [];
            if (!draft.main_group_order.includes(item.uuid)) {
                draft.main_group_order.push(item.uuid);
            }
            return draft;
        });
        setDeletedMainGroups((prev)=>prev.filter((i)=>i.uuid !== item.uuid || i.deletedAt !== item.deletedAt));
    };
    const removeCatalogGroup = (name)=>{
        // Archive data before deletion for Recycle Bin
        const catalogs = currentValues.catalog_groups?.[name] || [];
        const imageUrl = currentValues.catalog_group_image_urls?.[name] || "";
        // Find parent UUID
        let parentUUID = "";
        if (currentValues.subgroup_order) {
            parentUUID = Object.keys(currentValues.subgroup_order).find((uuid)=>Array.isArray(currentValues.subgroup_order[uuid]) && currentValues.subgroup_order[uuid].includes(name)) || "";
        }
        const parentName = parentUUID ? currentValues.main_catalog_groups?.[parentUUID]?.name || "General" : "General";
        setDeletedSubgroups((prev)=>[
                {
                    name,
                    catalogs,
                    imageUrl,
                    parentUUID,
                    parentName,
                    deletedAt: new Date().toISOString()
                },
                ...prev
            ]);
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["disableGroup"])(name, prev));
    };
    const unassignCatalogGroup = (name)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["unassignSubgroup"])(name, prev));
    };
    const assignCatalogGroup = (name, targetMainGroupUuid)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["assignSubgroup"])(name, targetMainGroupUuid, prev));
    };
    const addMainCatalogGroup = (name, assignedSubgroups)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createMainGroup"])(name, assignedSubgroups, prev));
    };
    const addCatalogGroup = (name, targetMainGroupUuid, imageUrl, initialCatalogs = [])=>{
        // Normalize IDs on addition to subgroup
        const normalized = initialCatalogs.map((id)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ensureCatalogPrefix"])(id, (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["resolveCatalogName"])(id, currentValues.custom_catalog_names || {})));
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSubgroup"])(name, targetMainGroupUuid, imageUrl, normalized, prev));
    };
    const importGroupsToState = (payload)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["importGroups"])(payload, prev));
    };
    const restoreSubgroup = (item)=>{
        setCurrentValues((prev)=>{
            const draft = JSON.parse(JSON.stringify(prev));
            // 1. Restore core data
            if (!draft.catalog_groups) draft.catalog_groups = {};
            draft.catalog_groups[item.name] = item.catalogs;
            if (item.imageUrl) {
                if (!draft.catalog_group_image_urls) draft.catalog_group_image_urls = {};
                draft.catalog_group_image_urls[item.name] = item.imageUrl;
            }
            // 2. Restore ordering references
            const targetUUID = item.parentUUID || Object.keys(draft.main_catalog_groups || {})[0];
            if (targetUUID) {
                if (!draft.subgroup_order) draft.subgroup_order = {};
                if (!Array.isArray(draft.subgroup_order[targetUUID])) draft.subgroup_order[targetUUID] = [];
                if (!draft.subgroup_order[targetUUID].includes(item.name)) {
                    draft.subgroup_order[targetUUID].push(item.name);
                }
                if (draft.main_catalog_groups?.[targetUUID]) {
                    if (!Array.isArray(draft.main_catalog_groups[targetUUID].subgroupNames)) {
                        draft.main_catalog_groups[targetUUID].subgroupNames = [];
                    }
                    if (!draft.main_catalog_groups[targetUUID].subgroupNames.includes(item.name)) {
                        draft.main_catalog_groups[targetUUID].subgroupNames.push(item.name);
                    }
                }
            }
            return draft;
        });
        setDeletedSubgroups((prev)=>prev.filter((i)=>i.name !== item.name || i.deletedAt !== item.deletedAt));
    };
    const clearDeletedSubgroups = ()=>{
        setDeletedSubgroups([]);
        setDeletedMainGroups([]);
    };
    // --- Manifest Catalog Mutations ---
    // These directly mutate the config.catalogs[] array
    const updateCatalogField = (id, patch)=>{
        setCatalogs((prev)=>{
            return prev.map((c)=>{
                if (c.id !== id) return c;
                const updated = {
                    ...c
                };
                for (const [key, val] of Object.entries(patch)){
                    if (key === 'metadata') {
                        updated.metadata = {
                            ...c.metadata,
                            ...val
                        };
                    } else {
                        updated[key] = val;
                    }
                }
                return updated;
            });
        });
    };
    const addManifestCatalog = (catalog)=>{
        const finalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ensureCatalogPrefix"])(catalog.id, catalog.name);
        setCatalogs((prev)=>{
            if (prev.find((c)=>c.id === finalId)) return prev; // No duplicates
            const newCat = {
                enabled: catalog.enabled ?? true,
                showInHome: catalog.showInHome ?? false,
                ...catalog,
                id: finalId
            };
            if (isSyntheticSession) {
                newCat._synthetic = true;
            }
            return [
                ...prev,
                newCat
            ];
        });
    };
    const removeManifestCatalog = (id)=>{
        // Soft delete: set enabled=false and showInHome=false
        setCatalogs((prev)=>prev.map((c)=>c.id === id ? {
                    ...c,
                    enabled: false,
                    showInHome: false
                } : c));
    };
    const reorderManifestCatalogs = (newCatalogs)=>{
        setCatalogs(newCatalogs);
    };
    const removeCatalog = (id)=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["disableCatalog"])(id, prev));
    };
    const countReferences = (name, isMainGroup = false)=>{
        return isMainGroup ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["countMainGroupReferences"])(name, currentValues) : (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["countGroupReferences"])(name, currentValues);
    };
    const cleanupOrphans = ()=>{
        setCurrentValues((prev)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["validateAndFix"])(prev));
    };
    const resetAll = ()=>{
        if (originalConfig) {
            loadConfig(originalConfig, fileName);
        }
    };
    const unloadConfig = ()=>{
        setOriginalConfig(null);
        setCurrentValues({});
        setInitialValues({});
        setCatalogs([]);
        setFileName("omni-config.json");
    };
    const exportConfig = ()=>{
        if (!originalConfig) return null;
        // 1. Start with deep clone of current decoded values
        let clonedValues = JSON.parse(JSON.stringify(currentValues));
        // 2. Prune explicitly disabled keys (from GenericRenderer toggles)
        clonedValues = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["pruneDisabledKeys"])(clonedValues, disabledKeys);
        // 3. Prune disabled catalogs (remove from arrays and entries)
        // Only prune if it's completely inactive (both shelf and top row disabled)
        const deadCatalogs = new Set(catalogs.filter((c)=>c.enabled === false && c.showInHome !== true).map((c)=>c.id));
        clonedValues = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["pruneDisabledCatalogs"])(clonedValues, deadCatalogs);
        // 4. Validate, Fix and Reorder keys
        // This also MUST happen while decoded
        const validatedValues = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["validateAndFix"])(clonedValues);
        // 5a. If manifest mode (config.catalogs[]) — real objects, not synthetic
        const finalResult = {
            ...originalConfig
        };
        const isSynthetic = isSyntheticSession;
        if (originalConfig.config && !isSynthetic && catalogs.length > 0) {
            // Manifest format: write catalogs back into config.catalogs[]
            const cleanCatalogs = catalogs.map((c)=>{
                const out = {
                    ...c
                };
                delete out._synthetic;
                return out;
            });
            // Also merge any side-array changes from currentValues (e.g. landscape_catalogs added by editor)
            // These live in currentValues but aren't catalog objects
            const sideArrayKeys = [
                'landscape_catalogs',
                'small_catalogs',
                'small_top_row_catalogs',
                'pinned_catalogs',
                'custom_catalog_names',
                'top_row_item_limits'
            ];
            const mergedConfig = {
                ...originalConfig.config || {}
            };
            for (const key of sideArrayKeys){
                if (currentValues[key] !== undefined) {
                    mergedConfig[key] = currentValues[key];
                }
            }
            finalResult.config = {
                ...mergedConfig,
                catalogs: cleanCatalogs
            };
            return finalResult;
        }
        // 5b. State format (or synthetic): encode values with updated state arrays
        // Sync the synthetic catalog state back to currentValues arrays before encoding
        let valuesToExport = {
            ...validatedValues
        };
        if (isSynthetic) {
            const activeIds = catalogs.map((c)=>c.id);
            const enabledIds = catalogs.filter((c)=>c.enabled !== false).map((c)=>c.id);
            const topRowIds = catalogs.filter((c)=>c.showInHome).map((c)=>c.id);
            const customNamesOut = {};
            const limitsOut = {};
            catalogs.forEach((c)=>{
                if (c.name && c.name !== c.id) customNamesOut[c.id] = c.name;
                if (c.metadata?.itemCount) limitsOut[c.id] = c.metadata.itemCount;
            });
            // Ensure selected_catalogs is always updated as it's the main reading source
            let finalEnabledIds = enabledIds;
            if (finalEnabledIds.length > 1 && finalEnabledIds.includes("omni_empty_setup_placeholder")) {
                finalEnabledIds = finalEnabledIds.filter((id)=>id !== "omni_empty_setup_placeholder");
            }
            valuesToExport.selected_catalogs = finalEnabledIds;
            // Also update catalog_ordering if the original config used it. It MUST contain all active catalogs (including hidden ones for top row)
            if (valuesToExport.catalog_ordering !== undefined) {
                valuesToExport.catalog_ordering = activeIds;
            }
            valuesToExport.top_row_catalogs = topRowIds;
            if (Object.keys(customNamesOut).length) valuesToExport.custom_catalog_names = {
                ...valuesToExport.custom_catalog_names || {},
                ...customNamesOut
            };
            if (Object.keys(limitsOut).length) valuesToExport.top_row_item_limits = limitsOut;
        }
        const originalValues = originalConfig.values || originalConfig.config || {};
        const encodedValues = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["encodeConfig"])(valuesToExport, originalValues, disabledKeys);
        if (originalConfig.values) {
            finalResult.values = encodedValues;
            finalResult.includedKeys = Object.keys(encodedValues);
        } else if (originalConfig.config) {
            finalResult.config = encodedValues;
        } else {
            finalResult.values = encodedValues;
            finalResult.includedKeys = Object.keys(encodedValues);
        }
        return finalResult;
    };
    const exportPartialConfig = (sectionKeys)=>{
        if (!originalConfig) return null;
        // Build a filtered values map containing only the specified section keys
        const filteredValues = {};
        for (const key of sectionKeys){
            if (currentValues[key] !== undefined) {
                filteredValues[key] = JSON.parse(JSON.stringify(currentValues[key]));
            }
        }
        // Also include main_group_order if exporting group keys
        if (sectionKeys.includes('main_catalog_groups') && currentValues.main_group_order) {
            filteredValues.main_group_order = JSON.parse(JSON.stringify(currentValues.main_group_order));
        }
        // Validate & fix the filtered subset
        const validatedValues = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$mutations$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["validateAndFix"])(filteredValues);
        // Encode using the original values for format detection
        const originalValues = originalConfig.values || originalConfig.config || {};
        const encodedValues = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$config$2d$utils$2e$ts__$5b$app$2d$client$5d$__$28$ecmascript$29$__["encodeConfig"])(validatedValues, originalValues, disabledKeys);
        // Build the full config shell
        const finalResult = {
            ...originalConfig
        };
        if (originalConfig.values) {
            finalResult.values = encodedValues;
            finalResult.includedKeys = Object.keys(encodedValues);
        } else if (originalConfig.config) {
            // Preserve config structure but only with partial values
            finalResult.config = encodedValues;
        } else {
            finalResult.values = encodedValues;
            finalResult.includedKeys = Object.keys(encodedValues);
        }
        return finalResult;
    };
    return /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["jsxDEV"])(ConfigContext.Provider, {
        value: {
            originalConfig,
            initialValues,
            currentValues,
            disabledKeys,
            disabledCatalogs,
            deletedSubgroups,
            deletedMainGroups,
            catalogs,
            fileName,
            isLoaded: !!originalConfig,
            loadConfig,
            updateValue,
            toggleKey,
            toggleCatalog,
            updateCatalogsOrder,
            updateCatalogField,
            addManifestCatalog,
            removeManifestCatalog,
            reorderManifestCatalogs,
            renameCatalogGroup,
            renameMainCatalogGroup,
            removeMainCatalogGroup,
            removeCatalogGroup,
            unassignCatalogGroup,
            assignCatalogGroup,
            addMainCatalogGroup,
            addCatalogGroup,
            importGroups: importGroupsToState,
            removeCatalog,
            countReferences,
            restoreSubgroup,
            restoreMainGroup,
            clearDeletedSubgroups,
            cleanupOrphans,
            resetAll,
            unloadConfig,
            exportConfig,
            exportPartialConfig,
            customFallbacks,
            setCustomFallbacks
        },
        children: children
    }, void 0, false, {
        fileName: "[project]/src/context/ConfigContext.tsx",
        lineNumber: 562,
        columnNumber: 9
    }, ("TURBOPACK compile-time value", void 0));
};
_s(ConfigProvider, "Xz+o+0eS+zOECOvIoGMArDHUN34=");
_c = ConfigProvider;
const useConfig = ()=>{
    _s1();
    const context = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$react$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["useContext"])(ConfigContext);
    if (!context) throw new Error("useConfig must be used within ConfigProvider");
    return context;
};
_s1(useConfig, "b9L3QQ+jgeyIrH0NfHrJ8nn7VMU=");
var _c;
__turbopack_context__.k.register(_c, "ConfigProvider");
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=src_b336ad8a._.js.map